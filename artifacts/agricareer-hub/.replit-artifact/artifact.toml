kind = "web"
previewPath = "/"
title = "AgriCareer Hub"
version = "1.0.0"
id = "artifacts/agricareer-hub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25818

[services.development]
run = "pnpm --filter @workspace/agricareer-hub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/agricareer-hub", "run", "build" ]
publicDir = "artifacts/agricareer-hub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25818"
BASE_PATH = "/"
